import { DisplayPasswordResetModalPipe } from './display-password-reset-modal.pipe';

describe('DisplayPasswordResetModalPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPasswordResetModalPipe();
    expect(pipe).toBeTruthy();
  });
});
