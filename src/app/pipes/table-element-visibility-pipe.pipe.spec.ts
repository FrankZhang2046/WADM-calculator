import { TableElementVisibilityPipePipe } from './table-element-visibility-pipe.pipe';

describe('TableElementVisibilityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TableElementVisibilityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
